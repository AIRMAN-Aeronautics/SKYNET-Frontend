export default function DispatchPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold capitalize">dispatch</h1>
      <p className="mt-2 text-muted-foreground">
        DispatchPage — connect to Skynet Backend to populate this view.
      </p>
    </div>
  );
}
